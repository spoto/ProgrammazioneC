// riempie arr, lungo length, con una struttura di parentesi casuale lunga length;
// si dia per scontato che length >= 0 e che length sia pari
void init_random(char arr[], int length);

// determina se arr, lungo length, e' una struttura di parentesi;
// si dia per scontato che length >= 0 e che length sia pari;
// QUESTA FUNZIONE DEVE ESSERE RICORSIVA
int struttura_di_parentesi(char arr[], int length);
