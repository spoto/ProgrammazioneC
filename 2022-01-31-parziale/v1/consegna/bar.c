#include <stdio.h>

// funzione ricorsiva che stampa n asterischi seguiti da 2*n chioccioline
void bar(int n) {
  // DA COMPLETARE, DEVE ESSERE RICORSIVA
  // si assuma che n sia non negativo
}

int main(void) {
  // DA COMPLETARE:
  // 1) legge n>= 0 da tastiera, richiedendolo a oltranza se negativo
  // 2) chiama bar(n)
  // 3) va a capo
  return 0;
}
